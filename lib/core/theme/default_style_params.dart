import 'package:flutter/widgets.dart';

abstract class Ds {
  static const double defaultMarginTriple = 48.0;
  static const double defaultMarginDouble = 32.0;
  static const double defaultMarginTwoThirds = 24.0;
  static const double defaultMargin = 16.0;
  static const double defaultMarginHalf = 8.0;
  static const double marginBetweenEl = 12.0;
  static const double appBarLinearProgressHeight = 8.0;
  static const EdgeInsets leftDefault = EdgeInsets.only(left: Ds.defaultMargin);
  static const EdgeInsets rightDefault = EdgeInsets.only(right: Ds.defaultMargin);
  static const EdgeInsets topDefault = EdgeInsets.only(top: Ds.defaultMargin);
}
